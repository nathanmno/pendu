import React from "react";
import "./App.css";

const Letter = () => {
  return (
    <div>
      {/* {alphabet.map((letter, key) => {
        return (
          <button key={"letter_" + key} onClick={() => action(letter)}>
            {letter}
          </button>
        );
      })} */}
    </div>
  );
};

export default Letter;
